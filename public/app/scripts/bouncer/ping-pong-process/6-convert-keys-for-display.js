/**
 * Step14 - /!\ #NUMERIC_COMPANY_KEYS contains 4 duplicate properties.
 * Merge `follower_count`, `employee_count_low` and `employee_count_high` of the lite version
 * with "full profile" as `staff_count_low`, `staff_count_high`, `num_followers`.
 * water down the keys so as to not make them obvious
 * */
